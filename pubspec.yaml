name: verification_code_flutter
homepage: https://github.com/honorjoey/verification_code_flutter.git
description: A Flutter package that help you create a verification input.
version: 1.0.0

environment:
  sdk: ">=2.16.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4